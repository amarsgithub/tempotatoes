# tempotatoes
## First do this

Name parent folder "PeerAdvisingSite".

### `Then`

Open git bash on the parent folder and do the following commands:

npm install

cd client

npm install

cd ..

npm run dev
